================================================================================
                    CHILD SAFETY PLATFORM — MASTER CONTEXT
================================================================================

Last Updated: 2026-09-12
Current Phase: Phase 6 COMPLETE (Phase 7 next)

This file contains the complete context for the AI-Powered Child Safety &
Protection Platform. Give this file to any AI model and it should understand
the product, architecture, features, implementation plan, and current progress.

================================================================================
SECTION 1: PROJECT OVERVIEW
================================================================================

PROJECT TYPE:
AI-enabled child safety, protection, support, and case-intelligence platform
for India.

PRIMARY GOAL:
Build a unified child-safety ecosystem that combines:
- Online child-safety monitoring
- Cyberbullying and threat detection
- Online grooming-risk detection
- Anonymous child reporting
- Professional human moderation
- Counselling/support
- Emergency SOS
- Parent/adult involvement
- Missing-child identification (CCTV-assisted)
- Cross-case intelligence ("connect the dots")
- Multilingual/regional-language support
- Persistent longitudinal child safety records
- Human-in-the-loop AI
- Privacy-preserving architecture

CORE PRINCIPLE:
AI detects, classifies, summarizes, retrieves, and connects information.
Humans (trained moderators, authorized authorities) verify and make decisions.
The system NEVER independently accuses, contacts law enforcement, or makes
irreversible child-protection decisions.

CORE INNOVATION:
1. Persistent moderator relationship — same moderator stays with a child
2. Longitudinal case intelligence — timeline, not isolated tickets
3. Cross-case "connect the dots" — knowledge graph linking verified cases

================================================================================
SECTION 2: TEAM STRUCTURE (3-Person Team)
================================================================================

PERSON 1 — Mobile/Android Engineer
  "I build what the child/parent uses."
  Owns: Kotlin Android app (Child Mode, Adult Mode, Shared Device Mode)

PERSON 2 — Web/Frontend Engineer (NOT our scope)
  "I build what moderators/police use."
  Owns: React/Next.js web portal (Moderator Portal, Authority Portal)

PERSON 3 — AI + Backend + Data Engineer
  "I build everything behind them that makes the system intelligent."
  Owns: FastAPI backend, AI/ML pipelines, databases, security

WE ARE BUILDING: Person 1 + Person 3
(Person 2's web portal is NOT in our scope)

Connection flow:
  Person 1 (Android App) → API → Person 3 (Backend/AI) → API/WS → Person 2 (Web)

================================================================================
SECTION 3: USER TYPES & APPLICATIONS
================================================================================

Four user roles:
1. CHILD — uses Android app (Child Mode)
2. ADULT / PARENT / GUARDIAN — uses Android app (Adult Mode)
3. MODERATOR — uses Web portal (Person 2's scope, but we build the APIs)
4. AUTHORITY / INVESTIGATOR — uses Web portal (Person 2's scope, but we build APIs)

Two applications:
1. ONE Android app with three modes (Child, Adult, Shared Device)
2. ONE Web app with two portals (Moderator, Authority) — Person 2 builds this

================================================================================
SECTION 4: ANDROID APP ARCHITECTURE
================================================================================

Single unified Kotlin Android app with Jetpack Compose.

MODES:
┌─────────────────────────────────────────────┐
│              UNIFIED ANDROID APP             │
├─────────────┬──────────────┬────────────────┤
│ CHILD MODE  │ ADULT MODE   │ SHARED DEVICE  │
├─────────────┼──────────────┼────────────────┤
│ Anonymous   │ Manage       │ Profile        │
│ reporting   │ children     │ switching      │
│ SOS         │ Emergency    │ Secure child   │
│ Emergency   │ alerts       │ session        │
│ location    │ Reporting    │ Separate       │
│ Counselling │ Location/    │ permissions    │
│ AI safety   │ emergency    │ Privacy        │
│ assistant   │ management   │ boundaries     │
│ Threat/     │ Child safety │                │
│ bullying    │ information  │                │
│ detection   │              │                │
│ Regional    │              │                │
│ language    │              │                │
│ Moderator   │              │                │
│ chat        │              │                │
│ Notifs      │              │                │
└─────────────┴──────────────┴────────────────┘

TECH STACK (Android):
- Kotlin, Android SDK, Jetpack Compose, Material 3
- Room + SQLite + DataStore (local storage)
- Retrofit + OkHttp + Moshi (networking)
- Kotlin Coroutines + Flow (async)
- Hilt (dependency injection)
- Navigation Compose (navigation)
- WorkManager + Foreground Services (background work)
- BiometricPrompt + Android Keystore (security)
- ONNX Runtime + ML Kit (on-device AI)
- Firebase Cloud Messaging (push notifications)
- WebSocket via OkHttp (real-time chat)
- Google Maps SDK (maps)
- CameraX (camera/media capture)

KEY ARCHITECTURAL DECISIONS:
- Identity-aware, not device-aware (shared device support)
- Child session is PIN-protected, adult session requires biometric/password
- Child-adult privacy boundaries enforced (child can report about a parent)
- Offline-first: reports queued locally, synced when online
- On-device AI first, server fallback for complex analysis
- Encrypted local storage for sensitive data

================================================================================
SECTION 5: BACKEND ARCHITECTURE
================================================================================

FastAPI backend serving APIs for both Android app and Web portal.

SERVICES/MODULES:
- Authentication (OAuth 2.0, JWT, role-based)
- User Management (CRUD for all user types)
- Child Management (protected child ID, privacy)
- Case Management (lifecycle, timeline, assignment)
- Incident Management (creation, AI flags, trust levels)
- Moderator Assignment (round-robin, persistent relationship)
- Reporting (anonymous, identified, voice, evidence)
- Evidence (upload, hash, provenance, verification)
- SOS (emergency events, location, notifications)
- Notifications (FCM push, WebSocket, in-app)
- AI Inference (proxy to AI server, result storage)
- Missing Child (case management, CCTV matching)
- Case Linking (cross-case intelligence, graph queries)
- Audit (comprehensive logging, tamper-resistant)

TECH STACK (Backend):
- Python 3.11, FastAPI, Pydantic, Pydantic-Settings
- SQLAlchemy (async) + asyncpg
- Alembic (migrations)
- python-jose + passlib (JWT + password hashing)
- WebSockets (real-time)
- boto3 (S3/MinIO)
- Firebase Admin SDK (push notifications)
- Redis (caching, sessions, queues)
- GeoAlchemy2 (PostGIS)
- pgvector (vector search)

================================================================================
SECTION 6: AI SERVER ARCHITECTURE
================================================================================

Separate FastAPI server for AI/ML inference.

NLP PIPELINE:
- Cyberbullying detection (XLM-R / MuRIL fine-tuned)
- Threat detection (severity levels, context-aware)
- Grooming-risk detection (conversation-level patterns)
- Language detection (Indian languages + code-mixed)
- Entity extraction
- Text classification
- Multilingual: Hindi, English, Hinglish, regional languages

COMPUTER VISION PIPELINE:
[NOTE: Heavy continuous CCTV video processing model is EXCLUDED to keep system lightweight]
- Missing child photo management & sighting uploads
- Lightweight image candidate verification for human review
- (Heavy YOLO/CLIP video stream processing deferred)

SPEECH PIPELINE:
- Speech-to-text (Whisper, Indic ASR)
- Text-to-speech (Indic TTS)
- Voice-based reporting

OCR PIPELINE:
- Screenshot → text (ML Kit, PaddleOCR)
- Feed to safety NLP

RISK ENGINE:
- XGBoost / LightGBM
- Combines: AI predictions + incident history + frequency + severity +
  moderator feedback + temporal trends → risk assessment

RAG + LLM:
- Case data → embeddings → pgvector → retrieval
- Permission-gated moderator queries
- LLM generates answers with source citations
- Case summarization

TECH STACK (AI):
- PyTorch, Hugging Face Transformers, Hugging Face Datasets
- XLM-R, MuRIL, IndicBERT (multilingual NLP)
- YOLO, OpenCV (computer vision)
- CLIP/SigLIP (visual embeddings)
- Whisper (speech-to-text)
- XGBoost, LightGBM, scikit-learn (risk engine)
- ONNX, ONNX Runtime (model export/optimization)
- FAISS / pgvector (similarity search)
- NumPy, Pandas

================================================================================
SECTION 7: DATABASE ARCHITECTURE
================================================================================

POSTGRESQL (primary relational):
Tables: Users, Children, Adults, Moderators, Cases, Incidents, Reports,
        Evidence, ModeratorNotes, SOSEvents, Notifications, Assignments,
        Escalations, AuditLogs, MissingChildren, ChatMessages

POSTGIS (geospatial, extension on PostgreSQL):
- SOS locations, incident locations, CCTV locations
- Missing-child last-known locations
- Proximity searches, geo-fencing

PGVECTOR (vector search, extension on PostgreSQL):
- Case embeddings, incident similarity
- Semantic search for RAG
- Image/text embeddings for missing child matching

NEO4J (knowledge graph):
Nodes: Child, Incident, Account, Location, Evidence, Time
Relationships: REPORTED_BY, OCCURRED_AT, INVOLVES_ACCOUNT, LINKED_TO
Purpose: Cross-case "connect the dots" capability

REDIS:
- Caching (risk scores, session data)
- Session management
- Job queues
- Real-time events (WebSocket pub/sub)
- Rate limiting

MINIO (S3-compatible object storage):
- Images, videos, screenshots, audio, evidence files

================================================================================
SECTION 8: SECURITY ARCHITECTURE
================================================================================

IDENTITY VAULT:
- Real identity encrypted (AES-256-GCM), stored separately
- Case system uses Protected Child ID (e.g., Child #C8291)
- Identity resolution only by authorized users with logged reason

ACCESS CONTROL:
- RBAC: role-based permissions (child, adult, moderator, authority, admin)
- ABAC: attribute-based (moderator sees only assigned children)

AUTHENTICATION:
- OAuth 2.0, JWT (access + refresh tokens)
- Biometric authentication on Android
- PIN for child session (shared device)

ENCRYPTION:
- TLS for transit
- AES-256-GCM for PII at rest
- Android Keystore for mobile secrets

AUDIT:
- Every sensitive action logged (who, what, when, resource, IP, reason)
- Tamper-resistant (append-only, hashed chain)

FOUR TRUST LEVELS:
1. RAW — original child submission
2. AI-DERIVED — prediction, classification, risk score
3. HUMAN-VERIFIED — moderator-confirmed
4. OFFICIAL — authority-confirmed

================================================================================
SECTION 9: KEY WORKFLOWS
================================================================================

CHILD REPORT WORKFLOW:
Child → anonymous report → privacy layer → case creation → AI analysis →
moderator notification → human review → support/verification/escalation

SOS WORKFLOW:
Child presses SOS → 3s countdown → capture GPS → create emergency event →
notify: linked adults + assigned moderator → emergency response → resolution

MISSING CHILD WORKFLOW:
Report filed → photo + description → AI generates embedding → CCTV uploads →
person detection (YOLO) → embeddings → similarity search → ranked candidates →
human verification

CROSS-CASE INTELLIGENCE:
New verified incident → extract entities → push to Neo4j → pattern detection →
shared accounts/locations/patterns found → flag for moderator/authority review

MODERATOR ASSISTANT (RAG):
Moderator asks question → permission check → case data retrieval from pgvector →
relevant context assembled → LLM generates answer with citations

AI FEEDBACK LOOP:
AI prediction → moderator decision → verified outcome → evaluation dataset →
quality control → aggregated dataset → model training → evaluation → deployment

================================================================================
SECTION 10: PROJECT STRUCTURE
================================================================================

Child Safety/
├── backend/                  # FastAPI backend server
│   ├── app/
│   │   ├── api/v1/           # Route handlers
│   │   ├── core/             # Config, security, dependencies
│   │   ├── models/           # SQLAlchemy ORM models
│   │   ├── schemas/          # Pydantic request/response schemas
│   │   ├── services/         # Business logic layer
│   │   ├── db/               # Database session, migrations
│   │   └── utils/            # Helpers, constants
│   ├── alembic/              # DB migrations
│   ├── tests/
│   ├── requirements.txt
│   ├── Dockerfile
│   └── main.py
│
├── ai_server/                # Separate AI/ML inference server
│   ├── app/
│   │   ├── api/              # AI inference endpoints
│   │   ├── models/           # Model loading & management
│   │   ├── pipelines/        # NLP, CV, Speech, OCR pipelines
│   │   ├── risk_engine/      # Risk scoring system
│   │   ├── rag/              # RAG + LLM pipeline
│   │   └── utils/
│   ├── model_store/          # Downloaded/trained model files
│   ├── tests/
│   ├── requirements.txt
│   ├── Dockerfile
│   └── main.py
│
├── android/                  # Kotlin Android app
│   └── ChildSafety/
│       ├── app/
│       │   └── src/main/
│       │       ├── java/com/childsafety/app/
│       │       │   ├── data/           # Repository, Room, DataStore
│       │       │   ├── domain/         # Use cases, models
│       │       │   ├── network/        # Retrofit, API clients
│       │       │   ├── ui/             # Compose screens & viewmodels
│       │       │   │   ├── child/      # Child mode screens
│       │       │   │   ├── adult/      # Adult mode screens
│       │       │   │   ├── shared/     # Shared device mode
│       │       │   │   ├── auth/       # Login/register
│       │       │   │   ├── common/     # Shared components
│       │       │   │   └── navigation/ # Nav graph
│       │       │   ├── service/        # Foreground services, FCM
│       │       │   ├── ai/             # On-device ONNX/ML Kit
│       │       │   ├── security/       # Encryption, Keystore
│       │       │   └── di/             # Hilt DI modules
│       │       └── res/
│       ├── build.gradle.kts
│       └── gradle/
│
├── docker-compose.yml        # PostgreSQL, Redis, Neo4j, MinIO
├── .env.example
└── context.txt               # THIS FILE

================================================================================
SECTION 11: IMPLEMENTATION PLAN (12 Phases)
================================================================================

Phase 1: Foundation & Project Scaffolding
  - All three project skeletons (backend, ai_server, android)
  - Docker infrastructure (PostgreSQL+PostGIS+pgvector, Redis, Neo4j, MinIO)
  - Database schemas + Alembic migrations
  - Health check endpoints
  - Android project with all dependencies configured
  STATUS: ✅ COMPLETE

Phase 2: Authentication & Identity System
  - JWT auth (register, login, refresh, logout)
  - Role-based access control
  - Identity vault (encrypted PII, protected child ID)
  - Android login/register screens
  - Secure token storage (Android Keystore)
  - BiometricPrompt integration
  STATUS: ✅ COMPLETE

Phase 3: User & Child Management + App Mode Selection
  - User/Child/Adult CRUD APIs
  - Adult-child linking with privacy boundaries
  - Android mode selection (Child/Adult/Shared)
  - Profile switching for shared devices
  - Onboarding flow
  STATUS: ✅ COMPLETE

Phase 4: Android UI — Child, Adult & Shared Mode Screens
  - Child Mode: Home, Report, Chat, Safety Tips, SOS, Settings
  - Adult Mode: Home, Children List, Add Child, Alerts, Report, Settings
  - Shared Device: Profile Switcher, Child Session Lock
  - Common components: TopBar, BottomNav, Loading/Error states
  STATUS: ✅ COMPLETE

Phase 5: Case & Incident Management
  - Case CRUD + lifecycle (open/investigating/resolved/escalated)
  - Incident management with AI flags + trust levels
  - Anonymous reporting API
  - Evidence upload to MinIO (hash, provenance)
  - Moderator notes/case journal
  - Auto case creation + moderator assignment
  STATUS: ✅ COMPLETE

Phase 6: SOS & Emergency System
  - SOS trigger + location capture (PostGIS)
  - Emergency event creation + notification chain
  - Proximity queries
  - Android: SOS foreground service, countdown, GPS
  - Adult: Emergency map with child location
  NEEDS: Google Maps API key
  STATUS: NOT STARTED

Phase 7: AI Pipeline — NLP Safety Detection
  - AI server with model loading infrastructure
  - Cyberbullying detection (XLM-R/MuRIL)
  - Threat detection (severity + context)
  - Grooming-risk detection (conversation patterns)
  - Language detection (Indian languages + code-mixed)
  - Backend integration: auto-flag reports, notify moderators
  NEEDS: GPU vs CPU decision, HuggingFace token (if gated models)
  STATUS: NOT STARTED

Phase 8: Notifications & Real-time Communication
  - Firebase Cloud Messaging (push notifications)
  - WebSocket chat (child ↔ moderator)
  - Notification management (CRUD, read status)
  - Android: FCM service, notification channels, chat UI
  NEEDS: Firebase credentials (google-services.json + Admin SDK key)
  STATUS: NOT STARTED

Phase 9: Missing Child Management & Lightweight Image Sightings
  - Missing child case management (photo, description, last known location)
  - Sighting report submission with photo attachment
  - Candidate listing for moderator/authority human verification
  - [EXCLUDED: Heavy CCTV continuous video frame processing & heavy YOLO/CLIP video stream pipeline excluded to avoid resource overhead]
  - Android: missing child alert viewing + sighting submission
  STATUS: NOT STARTED

Phase 10: Case Intelligence & Knowledge Graph
  - Neo4j integration (nodes, relationships, queries)
  - Risk engine (XGBoost/LightGBM)
  - Case similarity via embeddings + pgvector
  - Cross-case pattern detection
  - "Connect the dots" capability
  STATUS: NOT STARTED

Phase 11: RAG, LLM & Speech
  - RAG pipeline (embed → retrieve → generate)
  - LLM integration (permission-gated moderator assistant)
  - Case summarization
  - Speech-to-text (Whisper, Indic ASR)
  - Voice reporting (Android + backend)
  - AI Safety Assistant for children
  NEEDS: LLM API key (Gemini/OpenAI)
  STATUS: NOT STARTED

Phase 12: Security Hardening, On-device AI & Final Integration
  - Full RBAC + ABAC implementation
  - Audit logging (tamper-resistant)
  - Hardened identity vault (KMS integration point)
  - On-device AI (ONNX quantized models)
  - OCR (ML Kit) → safety analysis
  - Offline-first sync (WorkManager)
  - End-to-end integration testing
  STATUS: NOT STARTED

================================================================================
SECTION 12: OPEN QUESTIONS
================================================================================

1. GPU availability — Will the AI server run on GPU or CPU only?
2. LLM choice — API-based (Gemini/OpenAI) or local (Llama/Qwen/Gemma)?
3. Firebase project — Already set up or need to create?
4. Google Maps vs Mapbox — Which maps provider?
5. Deployment target — AWS, Azure, GCP, or local server?
6. Hackathon vs production — Optimize for demo or production-grade?
7. Model training — Have datasets or use pre-trained/zero-shot?

================================================================================
SECTION 13: CREDENTIALS TRACKER
================================================================================

| Credential                    | Status      | Needed By |
|-------------------------------|-------------|-----------|
| Google Maps API Key           | NOT PROVIDED | Phase 6   |
| HuggingFace Token             | NOT PROVIDED | Phase 7   |
| Firebase google-services.json | NOT PROVIDED | Phase 8   |
| Firebase Admin SDK Key        | NOT PROVIDED | Phase 8   |
| LLM API Key (Gemini/OpenAI)  | NOT PROVIDED | Phase 11  |

================================================================================
SECTION 14: PROGRESS LOG
================================================================================

[2026-09-12] Project initialized. Architecture finalized.
             Implementation plan created and approved by user.
             Created context.txt master context file.

[2026-09-12] PHASE 1 COMPLETE — Foundation & Scaffolding
             Files created: 76 total across 3 components
             
             Backend (33 files):
             - FastAPI app with CORS, lifespan, health check
             - Pydantic Settings config (DB, Redis, Neo4j, MinIO, JWT, AI)
             - Async SQLAlchemy engine + session factory
             - Base class with TimestampMixin + SoftDeleteMixin
             - 19 ORM models: User, Child, Adult, AdultChildLink, Moderator,
               Assignment, Case, Incident, Report, ModeratorNote, Evidence,
               SOSEvent, Notification, ChatMessage, Escalation, AuditLog,
               MissingChild, CCTVCandidate, IdentityVault
             - PostGIS Geometry columns (SOSEvent, MissingChild, CCTVCandidate)
             - pgvector Vector(512) columns (MissingChild, CCTVCandidate)
             - Alembic migration setup (async env.py)
             - Health check with DB + Redis connectivity test
             
             AI Server (19 files):
             - Separate FastAPI app on port 8001
             - Thread-safe singleton ModelRegistry (lazy loading, GPU/CPU)
             - Health endpoints (/health, /health/models, /health/gpu)
             - Pipeline stubs: NLP, CV, Speech, OCR
             - Risk engine + RAG package stubs
             
             Android (24 files):
             - Kotlin 2.0.20 + Jetpack Compose + Material 3
             - Version catalog with 40+ dependencies
             - Hilt DI, Room DB, Retrofit+OkHttp+Moshi networking
             - DataStore preferences (app mode, profile, language, auth)
             - Auth interceptor for JWT tokens
             - Child-friendly theme (soft blues, greens, warm accents)
             - WelcomeScreen placeholder
             - All permissions in manifest
             
             Infrastructure (4 files):
             - docker-compose.yml (PostgreSQL+pgvector, Redis, Neo4j, MinIO)
             - .env.example with all config vars
             - init-db.sql (PostGIS, pgvector, uuid-ossp, pgcrypto extensions)
             - .gitignore
             
             Next: Phase 2 — Authentication & Identity System
 
[2026-09-12] PHASE 2 COMPLETE — Authentication & Identity System
             Backend:
             - Core security (bcrypt password hashing, JWT access/refresh token creation & decoding)
             - Protected IDs generator (Protected Child ID: CXXXXXXXX, Case ID: CASE-XXXXXXXX)
             - Security encryption module: AES-256-GCM authenticated encryption with HKDF key derivation
             - IdentityVaultService: secure encryption & storage of sensitive child PII (name, phone, school, guardian)
             - Pydantic schemas: RegisterRequest (with 8-char + uppercase + digit password validation), LoginRequest, TokenResponse, UserProfile, UserUpdate
             - AuthService: async register, login, token refresh rotation, and logout with Redis token revocation / blacklist
             - FastAPI auth routes (/api/v1/auth/register, /login, /refresh, /logout, /me GET and PUT)
             - Fast, role-based access control dependency require_roles()
             - Unit tests in tests/test_auth_security.py
 
             Android:
             - AuthApi: Retrofit endpoints for register, login, refresh, logout, profile
             - AuthModels: Moshi JSON data classes for all auth requests/responses
             - TokenManager: EncryptedSharedPreferences backed by Android Keystore (MasterKey AES256-GCM)
             - BiometricHelper: BiometricPrompt integration for biometric login & authentication
             - AuthRepository: API wrapper with token persistence, error handling, IO coroutine dispatch
             - AuthViewModel: StateFlow architecture with synchronous validation (min 8 chars, uppercase, digit)
             - LoginScreen: Material 3 Compose screen with show/hide password, error handling, loading states
             - RegisterScreen: Material 3 Compose screen with role selector (Child / Adult), form validation
             - Navigation: Updated NavGraph with Welcome -> Login <-> Register -> Mode Selection routing
             - MainActivity: Auto-routes based on tokenManager.isLoggedIn()
             - NetworkModule: Provides AuthApi, TokenManager, OkHttpClient with AuthInterceptor
 
[2026-09-12] PHASE 3 COMPLETE — User & Child Management + App Mode Selection
             Backend:
             - Schemas for Child (ChildCreate, ChildUpdate, ChildOut, LinkChildRequest, ChildLinkOut)
             - Schemas for Adult (EmergencyContact, AdultCreate, AdultUpdate, AdultOut)
             - ChildService: async CRUD, protected_child_id generation, adult-child linking & querying
             - AdultService: async get/create adult, update contacts, manage emergency info
             - API routes:
               - /api/v1/children (POST /, GET /me, GET /{id}, PUT /{id}, POST /link, GET /{id}/adults)
               - /api/v1/adults (GET /me, PUT /me, GET /me/children)
               - /api/v1/users (GET /me, PUT /me, GET /{user_id})
 
             Android:
             - Configured USB tethering target: http://10.147.167.171:8000/
             - Enabled android:usesCleartextTraffic="true" in AndroidManifest.xml
             - DataStore preferences: APP_MODE, ACTIVE_PROFILE_ID, CHILD_PIN, SERVER_URL
             - Retrofit APIs: ChildApi, AdultApi, and Moshi models (ChildModels.kt)
             - UI ModeSelectionScreen: Material 3 Card-based selector for Child, Adult, and Shared Device modes
             - UI SharedProfileSwitcher: Toggle between Adult and Child session with biometric/PIN protection
             - Placeholder dashboards: ChildHomeScreen, AdultHomeScreen, SharedHomeScreen
             - Navigation: Seamless flow from Auth -> Mode Selection -> respective Home dashboard
 
             Fixes & Infrastructure Enhancements:
             - GitHub Actions CI build resolved:
               * Added vector shield icon drawables (ic_launcher_foreground.xml, ic_launcher_background.xml, adaptive mipmaps) to resolve AAPT linking error.
               * Created UserEntity & UserDao in AppDatabase to resolve Room 2.6+ KSP requirement ("@Database annotation must specify list of entities").
               * GitHub Actions workflow successfully compiled and published 48.99 MB ChildSafety-Debug-APK.
             - Backend Local Dev Runner (run_local_dev.py):
               * Created zero-dependency local runner supporting SQLite + in-memory Redis fallback so laptop can run backend without requiring Docker.
               * Fixed passlib/bcrypt 5.0 compatibility bug by switching to direct native bcrypt.
               * Added default=uuid.uuid4 to User/Child/Adult models for cross-DB UUID generation.
               * Pydantic settings configured with extra='ignore' for .env flexibility.
             - Physical Device Hardware Testing:
               * User installed APK directly on physical Android phone via USB tethering (laptop at 10.147.167.171:8000).
               * VERIFIED: Account registration on real phone -> live backend -> JWT generated & stored in Keystore.
               * VERIFIED: Login with registered credentials.
               * VERIFIED: Mode selection UI (Child Mode, Adult Mode, Shared Device/Hybrid Mode) fully functional.

[2026-09-12] PHASE 4 COMPLETE — Android UI — Child, Adult & Shared Mode Screens
             Android:
             - Common UI Components:
               * AppTopBar: Material 3 top bar with title, active mode badge chip, back navigation icon, and settings shortcut.
               * AppBottomNav: Role-aware navigation bar dynamically displaying Child tabs (Home, Report, Chat, Tips) or Adult tabs (Dashboard, Children, Alerts, Contacts).
             - Child Mode Screens:
               * ChildHomeScreen: Shield protection banner, Protected Child ID card with one-touch copy, large pulsing SOS button with 3-second abort timer, quick action navigation cards.
               * ChildReportScreen: Child-friendly multi-step report wizard with category dropdown (Cyberbullying, Inappropriate Message, Stranger, Feeling Unsafe), platform dropdown (WhatsApp, Instagram, Snapchat, School, Other), confidential description input, anonymous submission toggle, and confirmation dialog.
               * ChildChatScreen: Interactive safety helper chat bubble interface with quick suggestion chips ("Someone is bullying me", "I saw something scary", "How do I report?"), auto-scroll message list, and message input.
               * SafetyTipsScreen: Bite-sized interactive educational cards teaching password security, blocking/muting bullies, safe adults, and healthy screen time.
               * ChildSettingsScreen: Regional language selector (English, Hindi), trusted guardians list, and mode exit action.
             - Adult Mode Screens:
               * AdultHomeScreen: Protected children count overview, active child status cards, quick link child action, recent alerts card, emergency quick actions (Emergency Contacts, Helpline 1098), and settings.
               * LinkChildScreen: Protected Child ID lookup (CXXXXXXXX format) and relationship dropdown selector (Mother, Father, Guardian, Teacher) with privacy notice.
               * AdultAlertsScreen: Tab-filtered alert feed (All, Critical, High, Medium) with severity color coding, timestamps, and one-tap emergency action buttons.
               * EmergencyContactsScreen: Emergency contact list with quick dial buttons (Police 112, Childline 1098, custom contacts) and floating action button dialog to add new contacts.
             - Shared Device Mode Screens:
               * SharedHomeScreen: Dynamic profile switcher banner at the top, sandboxed child environment vs adult dashboard, and session locking mechanism.
               * SessionLockDialog: 4-digit PIN dialog (default PIN: 1234) ensuring children cannot exit Child Mode or tamper with adult settings on a shared device.
             - Unified AppNavGraph:
               * Complete hierarchical navigation routes connecting Welcome -> Auth (Login/Register) -> Mode Selection -> Child/Adult/Shared Home and all child sub-screens & adult sub-screens.

             Next: Phase 5 — Case & Incident Management

[2026-09-12] PHASE 5 COMPLETE — Case & Incident Management
             Backend:
             - Models & Database:
               * Added default=uuid.uuid4 to Case, Incident, Report, ModeratorNote, Evidence, Moderator, Assignment, Escalation for cross-DB & SQLite dev compatibility.
               * Child model: made user_id nullable to support anonymous/unregistered child safety reports.
               * Evidence model: tracks file_url, SHA-256 file_hash, file_size, media_type, mime_type, provenance metadata.
             - Schemas & Validation:
               * Case schemas (CaseStatus, CasePriority, CaseCreate, CaseUpdate, CaseOut, CaseDetailOut).
               * Incident schemas (IncidentType, IncidentSeverity, TrustLevel, IncidentCreate, IncidentUpdate, IncidentOut).
               * Report schemas (ReportCreate, ReportOut, ReportSubmissionResponse).
               * Evidence schemas (EvidenceOut, EvidenceUploadResponse).
               * Moderator & journal schemas (NoteType, ModeratorNoteCreate, ModeratorNoteOut, ModeratorOut, EscalationCreate, EscalationOut).
             - Core Services:
               * case_service: Case lifecycle transitions, longitudinal case attachment, auto case generation with unique CASE-XXXXXXXX IDs, persistent moderator relationship (child always retains assigned moderator), case journal notes, authority escalation.
               * incident_service: Incident creation, case auto-linking, trust levels, moderator verification.
               * report_service: Anonymous and authenticated reporting, automated triage into active cases, child PII protection.
               * evidence_service: SHA-256 cryptographic digest calculation, media type inference, local storage & MinIO compatibility.
             - API Endpoints (FastAPI):
               * /api/v1/cases (GET /, POST /, GET /{id}, PUT /{id}, POST /{id}/notes, GET /{id}/notes, POST /{id}/escalate).
               * /api/v1/incidents (GET /active, GET /{id}, POST /, PUT /{id}/verify).
               * /api/v1/reports (POST / for anonymous/authenticated child/parent reports, GET /, GET /{id}).
               * /api/v1/evidence (POST /upload for multipart evidence uploads).
             - Tests & Verification:
               * Automated tests in backend/tests/test_case_incident.py passing 100%.
               * Verified live HTTP report submission against running local dev server: generates CASE-XXXXXXXX and links incident.

             Android:
             - Network & Models:
               * ReportModels.kt: Moshi data classes for CreateReportRequest, ReportResponse, IncidentItem.
               * ReportApi.kt: Retrofit endpoints for submitting reports and fetching active incidents.
               * NetworkModule.kt: Provided ReportApi singleton in Hilt.
             - UI Integration:
               * ReportViewModel.kt: StateFlow-based submit action with progress state and live case confirmation.
               * ChildReportScreen.kt: Connected to live ReportApi; submits category, platform, details, and anonymous switch; displays server-assigned Protected Case ID upon receipt.
               * AdultAlertsViewModel.kt: Fetches live active incidents from /api/v1/incidents/active.
               * AdultAlertsScreen.kt: Renders live incidents from backend combined with priority alerts.

             Next: Phase 6 — SOS & Emergency System

[2026-09-12] PHASE 6 COMPLETE — SOS & Emergency System
             Backend:
             - Models & Database:
               * SafePointGeometry TypeDecorator in models/sos.py: PostGIS Geometry('POINT', 4326) on PostgreSQL with SQLite Text fallback for local zero-dependency dev.
               * Explicit latitude, longitude, and accuracy float columns for reliable cross-engine serialization and mathematical Haversine proximity calculations.
               * Added default=uuid.uuid4 to SOSEvent, Notification, ChatMessage models for UUID primary keys across all platforms.
             - Schemas & Validation:
               * SOSTriggerRequest (child_id, latitude, longitude, accuracy, location_address, message).
               * SOSResolveRequest (message/resolution note).
               * SOSEventOut (full SOS event payload with protected_child_id, protected_case_id, notified_guardians_count).
               * NotificationOut & NotificationList (user notifications feed).
             - Services & Proximity Engine:
               * sos_service:
                 - Haversine distance calculation (haversine_distance_km).
                 - SOS trigger workflow: automatically provisions a critical Case (CASE-XXXXXXXX), a critical Incident (EMERGENCY_SOS), dispatches emergency notifications to all linked adult guardians and assigned moderator, logs SOS event beacon.
                 - Proximity queries: get_nearby_sos_events filters active SOS alerts within given radius (km).
                 - Resolution workflow: resolve_sos updates event status to RESOLVED, marks linked critical case resolved with timestamps.
               * notification_service: get_user_notifications, mark_notification_read.
             - API Endpoints (FastAPI):
               * /api/v1/sos/trigger (POST): Instant SOS beacon activation.
               * /api/v1/sos/{id}/resolve (PUT): Resolution by child or adult guardian.
               * /api/v1/sos/active (GET): List of all currently unresolved SOS events.
               * /api/v1/sos/nearby (GET): Geo-radius search for nearby active SOS incidents.
               * /api/v1/notifications/ (GET): Notification feed for authenticated users.
               * /api/v1/notifications/{id}/read (PUT): Mark notification as read.
             - Dev Server & Tests:
               * run_local_dev.py: Added SOSEvent table creation.
               * backend/tests/test_sos_emergency.py: Automated test suite passing 100%.
               * Live dev server verified on 0.0.0.0:8000.

             Android:
             - Google Maps & Permissions:
               * Configured com.google.android.geo.API_KEY with user Maps key in AndroidManifest.xml.
               * Configured ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION, FOREGROUND_SERVICE_LOCATION permissions.
             - Network & Models:
               * SosModels.kt: TriggerSosRequest, ResolveSosRequest, SosEventResponse, NotificationItem, NotificationListResponse.
               * SosApi.kt: Retrofit endpoints for triggerSos, resolveSos, getActiveSos, getMyNotifications.
               * NetworkModule.kt: Provided SosApi singleton in Hilt DI container.
             - UI & State:
               * SosViewModel.kt: 3-second abortable countdown timer, GPS location acquisition via FusedLocationProviderClient (with fallback), SOS trigger network dispatch, active emergency state tracking, and resolution workflow.
               * ChildHomeScreen.kt: Interactive SOS button with 3-second countdown and abort tap; when active, turns into pulsing red emergency banner showing notified guardians count and protected case ID; "I Am Safe Now" resolution button; direct emergency dial shortcuts (Police 112, Childline 1098).
               * AdultAlertsViewModel.kt: Injected SosApi, aggregates live active SOS events and active incidents, parent resolution method.
               * AdultHomeScreen.kt: High-priority active emergency SOS card with child name, live coordinates, Google Maps intent (geo:lat,lng), Call Police 112, Call Childline 1098, and Resolve buttons.
               * AdultAlertsScreen.kt: Critical alert cards enhanced with one-touch Call 112 and Helpline 1098 quick action buttons.

             Next: Phase 7 — AI Safety Pipeline: Cyberbullying, Grooming & Content Moderation

================================================================================
END OF CONTEXT
================================================================================
